---
id: 20260814-134557-20260814-f2.5-alta-completadas-chat-fix-
type: summary
project: agente_ia
tags: [f2.5,alta,chat,onnlogin,orquestador,roadmap,atlas]
created: 2026-08-14T18:45:57+00:00
source: opencode
status: active
links:
---

Se completaron las 3 deudas ALTA de la fase F2.5 Endurecimiento (roadmap v1.4, orden: F2.5 → C1 → F4 → F5) el 20260814:

1. **CHAT (bug crítico de logon)**: `atlas_chat.py` usaba `import datetime` pero llamaba `datetime.now()` → NameError silencioso al arrancar por wscript (pythonw sin consola). El proceso moría ANTES de abrir el stderr log → stderr vacío sin diagnóstico. Fix: `from datetime import datetime`. Verificado: `opencode serve` arranca y responde en :4096/`:config`, stderr se escribe en logs/atlas_chat_stderr.log. 6 tests (tests/unit/test_chat.py) — binario de mayor versión (opencode.cmd 1.18.18 > exe 1.18.16), server_up, stop_server. Commit 6db3e2d.

2. **PROVIDERS ONLOGON**: Se creó `start_omniroute.vbs` (replica el comando real `node --max-old-space-size=4096 dist\server-ws.mjs`, puerto default :20128) + tarea `AtlasOmniRoute` ONLOGON (restart 3/1min, IgnoreNew). 9Router ya tenía `start_9router.vbs` + `Atlas9Router` ONLOGON en `--port 4000` (evita colisión :20128). Ambos providers con auto-start.

3. **ORQUESTADOR DEFAULT**: `opencode.jsonc` ya tiene `"model": "auto/best-coding"` (default del orquestador, cubierto por omniroute con 260 modelos) + MCP atlas-orchestrator enabled. Fix de consistencia: `vision_supported` ahora presente en TODAS las ramas de decision (block_and_advise=False, no_providers=False, proceed=bool). Esto eliminó un test flaky.

**F2.5 queda pendiente**: gates de verificación final — `AtlasChat` sobrevive 3 reboots (requiere reiniciar y observar el log), `atlas_supervisor` probado contra daemon matado, restore cifrado probado (el backup ya está, falta un restore real), origin/git remote aún sin URL del usuario.

**Siguiente fase**: C1 Ejecución Profesional (SPEC_C1.html, 6 skills, windows MCP screen_capture/OCR, /api/informes, estándar 6 bloques).
